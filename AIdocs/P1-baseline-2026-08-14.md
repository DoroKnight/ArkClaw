# P1 — 基线验证记录（2026-08-14）

> 工作树：`.worktrees/arkpets-spine-idle-vertical-slice` @ `7a6fb3c`（P0 基线提交，工作树干净）

## 运行方式（与交接文档 §16.5 一致）
- 解释器：工作树 `.venv`（Python 3.13.6，uv 0.11.2 创建）
- 环境：`PYTHONPATH=<工作树>\src`
- pytest basetemp 指向仓库内 `build\.pytest-tmp-*`（沙箱禁止写系统 `%LOCALAPPDATA%\Temp`）

## 结果

### pytest（全套）
- **3053 passed, 10 skipped, 10 failed**
- 说明：交接摘要中记录的 477 errors 全部是 `tmp_path` fixture 写系统 TEMP 被沙箱拒绝所致（`PermissionError WinError 5`），与代码无关；在正确环境下不出现。

### 10 failed（全部为环境相关，非代码回归）
| 测试 | 类别 | 原因 |
|---|---|---|
| `qt/test_pet_mesh_opengl_backend.py::test_real_windows_backend_smoke_and_metrics` | Qt/GL | 需要真实 Windows OpenGL 显示环境 |
| `qt/test_pet_production_actions.py::test_tray_exposes_seven_typed_actions_...` | 托盘 | 真实托盘不可用 |
| `qt/test_pet_window.py::test_pet_smoke_isolates_inherited_qt_environment` | Qt 窗口 | 窗口/环境隔离子进程 |
| `qt/test_pet_window.py::test_qt_tray_view_builds_reviewed_menu_...` | 托盘/菜单 | 菜单内容与真实环境差异 |
| `qt/test_pet_window.py::test_tray_smoke_uses_fake_tray_...` | 托盘 | 托盘资源清理 |
| `qt/test_pet_window.py::test_right_click_exit_waits_for_shutdown_result` | 右键退出 | 等待真实关闭流程 |
| `qt/test_single_instance.py::test_single_instance_smoke_uses_isolated_two_process_boundary` | 子进程 | 子进程 Qt 平台插件/字体环境（返回码 2） |
| `unit/test_standalone_packaging.py::test_standalone_attempt_archive_...` ×3 | 打包 | 依赖真实 Nuitka standalone 构建证据 + `.venv-packaging` 打包环境 |

### ruff
- `ruff check src tests scripts packaging` → **通过（All checks passed）**

### mypy
- `mypy src tests scripts packaging` → **49 errors，全部在 7 个测试文件**（`test_pet_window.py` 40、`test_pet_effect_overlay.py` 6、`test_schwarz_native_input.py` 5、`test_pet_explicit_action_control.py` 3、`test_spine38_schwarz_smoke.py` 2、`test_spine38_native.py` 1、`test_pet_production_actions.py` 1）
- `src/`、`scripts/`、`packaging/` → **0 errors**（生产代码 mypy 干净）
- mypy 2.3.0 与 uv.lock 一致；`[tool.mypy] strict=true` 全局生效

## 基线结论
- 生产代码静态与运行时基线干净；10 个失败全部可归因于运行环境（无交互桌面、无托盘、无真实 OpenGL、无 Nuitka 构建证据），与代码无关。
- **P2 成功标准**：pytest 失败集合不劣于基线（仍为 ≤10 个环境相关失败，且无新增）；`src/` mypy 0 error；ruff 全过。
